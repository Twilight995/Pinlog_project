# 📍 Pinlog 발표 대본

> 발표일: 2026-06-11  |  예상 시간: 10~12분

---

# 🎬 HERO — 인트로

안녕하세요. 저희 팀은 위치 기반 감정 일기 앱 **Pinlog**를 개발했습니다.

Pinlog는 한 문장으로 정리하면, **"지도 위에 기억을 심는 앱"** 입니다.
핀 생성 6단계 위자드, 12가지 감정 타입, 3D 지구본 뷰, 실시간 소셜 연결까지 갖춘 iOS 앱입니다.

---

# 01 / 문제점 발견

여행을 다녀오면 수백 장의 사진을 찍습니다.
하지만 시간이 지나면 **"그게 어디였더라, 그때 뭘 먹었더라"** 라는 기억이 흐릿해지죠.

기록 자체도 파편화돼 있습니다.
사진은 카메라 롤, 감상은 메모 앱, 위치는 지도 앱에 **따로따로** 흩어져 있어서 나중에 돌아보기가 어렵습니다.

그리고 함께 여행한 친구와 그 기억을 공유하거나, 추천 장소를 위치와 함께 자연스럽게 전달하는 방법도 마땅치 않습니다.

## 경쟁사 대비 차별점

기존 서비스들을 보면,
여행 일기 앱은 위치 기록은 되지만 감정이나 소셜이 약하고,
인스타그램은 공유는 되지만 위치 기반 아카이빙이 아닙니다.

Pinlog는 **위치 + 감정 + 소셜** 세 가지를 하나의 앱에서 통합합니다.
이게 핵심 차별점입니다.

---

# 02 / 개발 스택

- **Flutter** — Riverpod v2로 전역 상태 관리, Hive로 오프라인 우선 캐싱. iOS 단일 코드베이스.
- **Mapbox** — 3D 글로브 모드, 핀 클러스터링. 국가 단위로 핀이 묶이고 확대하면 개별 핀으로 펼쳐짐.
- **Supabase** — PostgreSQL 기반 백엔드. Realtime으로 실시간 위치 공유, RLS 정책으로 계정 간 데이터 접근 완전 격리.
- **Firebase FCM** — Admin SDK 없이 HTTP v1 API 직접 호출 방식으로 푸시 알림 구현.
- **Gemini Vision** — 사진 업로드 시 이미지 분석 → 핀 제목 자동 추천.

---

# 03 / 문제 해결

개발하면서 세 가지 큰 기술 문제가 있었습니다.

## 🔴 01. Mapbox 핀 잔상 버그

핀을 삭제하고 새로 생성할 때, 실기기에서만 이전 핀의 잔상이 남는 현상이 있었습니다.

- **원인** — `deleteAll()` 후 `createMulti(opacity: 0.0)`으로 생성하는 페이드인 애니메이션
- **해결** — opacity를 1.0으로 바로 생성하고 뮤텍스 지연 제거

## 🔴 02. OAuth 로그인 콜백 누락

Google 로그인 시 Safari에서 앱으로 돌아올 때 세션이 null로 오는 문제가 있었습니다.

- **원인** — URL Scheme이 Info.plist에 미등록
- **해결** — `io.supabase.pinlog` URL Scheme 추가 + `isOAuthWaiting` 가드 적용

## 🔴 03. Firebase Admin SDK 미지원 환경

Supabase Edge Function은 Deno 런타임을 사용하는데, Firebase Admin SDK가 Deno를 지원하지 않습니다.

- **원인** — Deno 환경에서 Firebase Admin SDK 설치 불가
- **해결** — Service Account JSON → `crypto.subtle.sign`으로 JWT 직접 발급 → FCM HTTP v1 API 직접 호출

---

# 04 / 데모

> 이 시점부터 실기기 또는 웹 데모 화면으로 전환

## 핀 생성

지도를 길게 누르면 핀 생성 위자드가 시작됩니다.
카테고리 선택, 감정과 강도 선택, 날씨 자동 감지, 동행자 입력, 사진 첨부까지 6단계로 진행됩니다.
날씨는 현재 위치 기반으로 자동 감지되어 선택됩니다.
사진을 첨부하면 Gemini가 이미지를 분석해 제목을 추천합니다.
저장하면 지도 위에 핀이 심어집니다.

## 소셜 기능

친구 추가는 고유 코드로 합니다.
친구 신청을 보내면 상대방에게 FCM 푸시 알림이 전송됩니다.
앱이 열려있으면 커스텀 인앱 배너로 표시됩니다.

## 리캡 알림

1년 전 오늘 방문했던 장소가 있으면, 앱 실행 시 "오늘의 추억" 알림이 뜨고 리캡 팝업이 표시됩니다.

## 3D 지구본

지도를 축소하면 3D 지구본 모드로 전환됩니다.
전 세계에 심은 핀들을 한눈에 볼 수 있습니다.

---

# 05 / Q&A

이상으로 Pinlog 발표를 마치겠습니다.
질문 있으시면 받겠습니다. 감사합니다.

---

# 💬 예상 질문 & 답변

## Q. 실제 사용자 데이터는 어떻게 보호하나요?

Supabase RLS 정책으로 각 사용자는 자신의 핀만 읽고 쓸 수 있습니다. 친구 공개 핀은 친구 관계가 확인된 경우에만 조회 가능합니다.

## Q. 왜 Firebase 대신 Supabase를 선택했나요?

Firebase는 NoSQL이라 관계형 쿼리가 복잡해집니다. Supabase는 PostgreSQL 기반이라 친구 관계, 핀 공개 범위 같은 복잡한 관계형 쿼리를 훨씬 깔끔하게 처리할 수 있었습니다.

## Q. Gemini Vision은 어떻게 연동했나요?

핀 생성 위자드 마지막 단계에서 사진을 업로드하면 Gemini Vision API를 호출해 이미지를 분석하고, 어울리는 제목 후보를 반환합니다. 사용자는 추천된 제목을 선택하거나 직접 입력할 수 있습니다.

## Q. 앱 성능은 어떻게 최적화했나요?

Hive로 로컬 캐싱을 구현해 오프라인에서도 기존 핀을 조회할 수 있습니다. Mapbox 핀 마커는 변경된 핀만 선택적으로 업데이트해서 불필요한 리렌더링을 막았습니다.
